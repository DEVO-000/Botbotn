python-telegram-bot[job-queue]==21.6
aiohttp>=3.9
groq>=0.11
pymongo>=4.6
# === СЕЙФ: ДО 2 ГБ (волна 14) — гибрид Bot API + MTProto ===
# Telethon — MTProto-клиент под тем же бот-токеном: качает и заливает файлы
# до 2 ГБ (Bot API умеет только 20 МБ вниз / 50 МБ вверх). Без библиотеки
# бот продолжит работать: файлы ≤20 МБ шифруются как раньше, а на большие
# бот честно скажет, что режим ещё не включён.
telethon>=1.36
# Cryptg — быстрая C-реализация AES для Telethon: без него 2 ГБ заливаются
# часами (чистый Python), с ним — минуты. Не обязателен: без него Telethon
# работает медленнее, но работает.
cryptg>=0.4
# === СЕЙФ (шифрованное хранилище, волна 7) ===
# AES-256-GCM для шифрования файлов паролем пользователя. Без библиотеки
# бот продолжит работать, но кнопка «🔐 Сейф» честно скажет, что нужно
# выполнить: pip install cryptography
cryptography>=42.0
# === OCR (распознавание текста на фото, бесплатно и безлимитно) ===
# Основной движок — Tesseract: НУЖЕН ещё системный пакет:
#   Debian/Ubuntu:  apt install tesseract-ocr tesseract-ocr-rus
#   (для английского добавьте tesseract-ocr-eng)
pytesseract>=0.3.10
Pillow>=10.0
# Альтернативный движок (тяжелее, ~1 ГБ с torch, модели качает сам):
#   pip install easyocr
# easyocr>=1.7
# Языки OCR настраиваются переменной окружения OCR_LANGS (по умолчанию rus+eng).
# Если ни один OCR-движок не установлен — фото обрабатываются вижн-моделью Groq,
# бот при этом продолжает работать без ошибок.
# === ОПИСАНИЕ ИЗОБРАЖЕНИЙ для DeepSeek (бесплатно и безлимитно) ===
# DeepSeek работает только с текстом. Чтобы он «видел», ЧТО изображено на
# фото (мем, схема, рукопись), установите локальную модель BLIP (~1 ГБ,
# качается один раз, дальше офлайн):
#   pip install transformers torch
# Модель можно заменить переменной окружения PHOTO_CAPTION_MODEL
# (по умолчанию Salesforce/blip-image-captioning-base); "" — отключить.
# Без transformers бот продолжает работать: OCR-текст и вижн-модель Groq.
